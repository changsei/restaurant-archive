<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>회원 가입</title>
    <script>
        // 비밀번호 강도 검사 함수
        function validatePassword() {
            var passwordInput = document.getElementById("password");
            var strengthIndicator = document.getElementById("strengthIndicator");
            var password = passwordInput.value;
            var regex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[!@#\$%\^&\*])(?=.{8,})/;

            if (regex.test(password)) {
                strengthIndicator.textContent = "강력한 비밀번호입니다.";
                strengthIndicator.style.color = "green";
                return true;
            } else {
                strengthIndicator.textContent = "비밀번호가 충분히 강력하지 않습니다. 대문자, 소문자, 숫자, 특수문자를 포함한 8자 이상 사용하세요.";
                strengthIndicator.style.color = "red";
                return false;
            }
        }
    </script>
</head>
<body>
    <h1>회원 가입</h1>
    <form action="/signup" method="POST" onsubmit="return validatePassword();">
        <div>
            <label for="userId">아이디:</label>
            <input type="text" id="userId" name="user_id" required>
        </div>
        <div>
            <label for="nickName">닉네임:</label>
            <input type="text" id="nickName" name="user_nick_name" required>
        </div>
        <div>
            <label for="email">이메일:</label>
            <input type="email" id="email" name="user_email" required>
        </div>
        <div>
            <label for="password">비밀번호:</label>
            <input type="password" id="password" name="user_password_hash" required oninput="validatePassword();">
            <p id="strengthIndicator"></p>
        </div>
        <div>
            <button type="submit">회원 가입</button>
        </div>
    </form>
    <a href="/">홈으로 돌아가기</a>
</body>
</html>